seed: 42

model:
  image_size: 56
  patch_size: 14
  embed_dim: 48

data:
  frame_skip: 1
  sequence_length: 2

training:
  batch_size: 4
  num_epochs: 2
  learning_rate: 0.001
  weight_decay: 0.05

loss:
  sigreg_weight: 0.09